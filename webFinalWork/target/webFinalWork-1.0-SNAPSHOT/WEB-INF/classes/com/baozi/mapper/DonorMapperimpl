<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.baozi.mapper.DonorMapper">
    <insert id="insert" parameterType="UserDonor" >
        insert into user_donor(donor_uuid,donor_username,donor_password,donor_mailbox)
        values(#{donorUuid},#{donorUsername},{donorPassword},#{donorMailbox})
    </insert>

</mapper>